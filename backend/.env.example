# ============================================================
# Paroisse Saint-Benoît — Variables d'environnement
# ============================================================
# Copiez ce fichier en ".env" et renseignez vos valeurs.
# Ne commitez JAMAIS le fichier .env dans git !

# Port du serveur (défaut : 3000)
PORT=3000

# Mot de passe du panneau d'administration
# Choisissez un mot de passe fort (min. 12 caractères)
ADMIN_PASSWORD=changez-moi-12345

# Secret pour les sessions (chaîne aléatoire longue)
# Générez-en un avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=remplacez-par-une-chaine-aleatoire-longue
